# 이 강의 어때? — 당신이 할일 / Cursor AI 할일

> 개발 시작용 역할 구분 체크리스트

---

## ⚠️ 개발 전 필수 — .env 올리지 않기

| 항목 | 설명 |
|------|------|
| **막아둔 것** | `.gitignore`에 `.env`, `.env.*` 가 있어서 **실수로 add 되어도 원격에 올라가지 않음** |
| **절대 하지 말 것** | `.env` 파일을 GitHub에 push 하지 마세요. Supabase key 등이 들어 있으면 유출됩니다. |
| **push 전 확인** | `git status` 했을 때 `.env`가 목록에 **없는지** 한 번 확인하고 push 하세요. |

`.env`는 로컬/Vercel/ GitHub Secrets에만 두고, **실제 키 값은 `.env.example`에 넣지 마세요.** (키 이름만 예시로 둔 상태입니다.)

---

## ✅ Cursor AI가 한 일 (완료)

- [x] `.gitignore` 생성
- [x] `.env.example` 생성
- [x] `vercel.json` 생성
- [x] `automation/crawler.py` 생성
- [x] `automation/requirements.txt` 생성
- [x] `.github/workflows/crawler.yml` 생성

**→ 이제 당신이 할 일만 남았습니다.**

---

## 📋 당신이 할 일 (순서대로)

### 1. GitHub (약 15분)

- [ ] github.com 로그인
- [ ] 새 리포지토리 생성 (이름 예: `igangui`, Public 권장)
- [ ] README 추가 후 생성
- [ ] 로컬에서 `git init` → `git add .` → `git commit` → `git remote add origin` → `git push -u origin main`

### 2. Vercel (약 15분)

- [ ] vercel.com 로그인
- [ ] **Add New Project** → GitHub 연결 → `igangui` 리포 선택
- [ ] Framework Preset: **Other** → **Deploy**
- [ ] 배포된 URL 접속해서 기존 화면 정상 노출 확인

### 3. Supabase 대시보드 (약 25분)

- [ ] **RLS 확인**: Table Editor → `reviews`, `creator_stats`, `courses` 각각 RLS 활성화 및 정책 확인
- [ ] **SQL 실행 1** (SQL Editor): `source`, `auto_detected_at` 컬럼 추가  
  → 지시서 3-3절 SQL 복사해서 실행
- [ ] **SQL 실행 2** (선택): 중복 방지 UNIQUE 제약  
  → 지시서 8절 "중복 강의 방지 SQL" 복사해서 실행

### 4. YouTube API + Make.com (약 1시간, 자동화 메인)

- [ ] Google Cloud Console에서 프로젝트 생성 → YouTube Data API v3 사용 설정 → API 키 발급
- [ ] Make.com 가입 → 새 시나리오: Schedule → HTTP(YouTube API) → Iterator → Filter → Supabase Insert Row 연결
- [ ] Supabase Insert 시 `title`, `creator_id`, `source`, `auto_detected_at` 매핑

### 5. GitHub Secrets (약 5분)

- [ ] 리포지토리 **Settings** → **Secrets and variables** → **Actions**
- [ ] `SUPABASE_URL` 추가 (Supabase 프로젝트 URL)
- [ ] `SUPABASE_SERVICE_KEY` 추가 (Supabase → Project Settings → API → service_role 키)

### 6. 크롤러 설정 (선택, 방법 B 사용할 때만)

- [ ] `automation/crawler.py` 안의 `TARGETS`에 실제 `creator_id`(UUID), `url` 입력
- [ ] GitHub Actions에서 **Actions** 탭 → **Weekly Crawler** → **Run workflow** 로 수동 1회 테스트

---

## 📁 현재 프로젝트 구조

```
igangwhat/
├── index.html              ← 기존 (수정 없음)
├── .gitignore              ← Cursor 생성
├── .env.example            ← Cursor 생성
├── vercel.json             ← Cursor 생성
├── automation/
│   ├── crawler.py          ← Cursor 생성 (TARGETS만 당신이 수정)
│   └── requirements.txt    ← Cursor 생성
├── .github/workflows/
│   └── crawler.yml         ← Cursor 생성
├── 이강의어때_개발지시서.md
└── 할일_구분.md            ← 이 파일
```

---

**다음 단계:** 위 "당신이 할 일" 1번(GitHub 리포 생성 + push)부터 진행하면 됩니다.
