# 🤔 이 강의 어때? — 개발 전환 & 자동화 추가 지시서

> **GitHub · Supabase · Vercel 아키텍처 기준** | 2026년 2월 작성

---

## 목차

- [0. 개요 및 목적](#0-개요-및-목적)
- [1. 사전 준비 및 계정 세팅](#1-사전-준비-및-계정-세팅)
- [2. GitHub 리포지토리 세팅](#2-github-리포지토리-세팅)
- [3. Supabase 보완 작업](#3-supabase-보완-작업)
- [4. Vercel 배포 연결](#4-vercel-배포-연결)
- [5. 자동화 파이프라인 구축](#5-자동화-파이프라인-구축)
- [6. 전체 작업 순서 요약](#6-전체-작업-순서-요약)
- [7. 기존 기능 유지 확인 체크리스트](#7-기존-기능-유지-확인-체크리스트)
- [8. 보안 권장사항](#8-보안-권장사항)
- [9. 참고 자료 및 공식 문서](#9-참고-자료-및-공식-문서)

---

## 0. 개요 및 목적

본 지시서는 현재 단일 `index.html` 파일로 운영 중인 **'이 강의 어때?'** 서비스를 GitHub + Supabase + Vercel 기반의 정식 웹 프로젝트 구조로 전환하고, 강의 데이터 자동화 파이프라인을 추가하기 위한 개발 가이드입니다.

> ⚠️ **기존 기능은 단 하나도 변경하거나 삭제하지 않습니다. 이 지시서는 추가 및 이전만 다룹니다.**

### 현재 상태 (As-Is)

| 항목 | 현재 상태 |
|------|----------|
| 프론트엔드 | 단일 index.html (HTML/CSS/JS 인라인) |
| 백엔드/DB | Supabase (이미 사용 중) |
| 배포 | 미상 (Cloudflare 경유 추정) |
| 강의 데이터 입력 | 수동 (이메일 요청 방식) |
| 버전 관리 | 없음 |

### 목표 상태 (To-Be)

| 항목 | 목표 상태 |
|------|----------|
| 프론트엔드 | Vercel로 배포된 정적 사이트 (기존 HTML 그대로) |
| 백엔드/DB | Supabase (동일, RLS 보완) |
| 배포 | GitHub → Vercel 자동 배포 (push하면 자동 반영) |
| 강의 데이터 입력 | YouTube API + Make.com 자동화 / 크롤러 보조 |
| 버전 관리 | GitHub 리포지토리 |

---

## 1. 사전 준비 및 계정 세팅

아래 계정이 모두 준비되어 있어야 합니다. **모두 무료 플랜으로 시작 가능합니다.**

| 서비스 | 용도 | 주소 |
|--------|------|------|
| GitHub | 코드 저장 & 버전 관리 | github.com |
| Vercel | 프론트엔드 배포 (자동) | vercel.com |
| Supabase | DB & API (기존 사용 중) | supabase.com |
| Make.com | YouTube API 자동화 워크플로 | make.com |
| Google Cloud | YouTube Data API v3 키 발급 | console.cloud.google.com |

---

## 2. GitHub 리포지토리 세팅

### 2-1. 리포지토리 생성

1. github.com 에서 새 리포지토리 생성
2. 리포지토리 이름: `igangui` (또는 원하는 이름)
3. Public 또는 Private 선택 (Vercel 무료 플랜은 Public 권장)
4. README.md 추가 체크 후 생성

### 2-2. 폴더 구조

아래 구조로 파일을 구성합니다. **기존 index.html은 루트에 그대로 배치합니다.**

```
igangui/
├── index.html            ← 기존 파일 그대로 (수정 없음)
├── .env.example          ← 환경변수 예시 파일
├── .gitignore            ← .env 등 민감 파일 제외
├── vercel.json           ← Vercel 배포 설정
└── automation/           ← 자동화 스크립트 폴더
    ├── crawler.py        ← 크롤러 (보조 방법)
    └── requirements.txt
```

### 2-3. `.gitignore` 파일 내용

```
.env
__pycache__/
*.pyc
node_modules/
```

> ⚠️ **주의:** Supabase Key 등 민감 정보는 절대 GitHub에 올리지 마세요. `.env` 파일은 `.gitignore`에 반드시 포함해야 합니다.

### 2-4. `.env.example` 파일 내용

실제 값은 넣지 않고 키 이름만 기록해 팀원에게 어떤 환경변수가 필요한지 알려주는 파일입니다.

```
SUPABASE_URL=your_supabase_project_url
SUPABASE_SERVICE_KEY=your_service_role_key
YOUTUBE_API_KEY=your_youtube_api_key
```

---

## 3. Supabase 보완 작업

### 3-1. 현재 이슈: Anon Key 노출

기존 `index.html`에 `SUPABASE_KEY`(anon key)가 하드코딩되어 있습니다. Anon key는 공개되어도 괜찮지만, **반드시 RLS(Row Level Security)가 올바르게 설정되어 있어야 합니다.**

### 3-2. RLS 정책 확인 체크리스트

- [ ] Supabase 대시보드 → Table Editor → `reviews` 테이블
- [ ] RLS 활성화(Enable RLS) 확인
- [ ] INSERT 정책: 누구나 후기를 작성할 수 있도록 허용
- [ ] DELETE/UPDATE 정책: 인증된 관리자만 가능하도록 제한
- [ ] `creator_stats` 테이블: SELECT만 허용 (anon 사용자)
- [ ] `courses` 테이블: SELECT 허용, INSERT는 service_role key만 가능하도록 설정

### 3-3. 자동화를 위한 테이블 컬럼 추가

자동화 파이프라인이 강의를 등록할 때 출처를 기록하기 위해 컬럼을 추가합니다.  
**Supabase 대시보드 → SQL Editor에서 아래 SQL을 실행합니다.**

```sql
-- courses 테이블에 자동화 출처 컬럼 추가
ALTER TABLE courses
  ADD COLUMN IF NOT EXISTS source TEXT DEFAULT 'manual';
  -- 값 예시: 'manual', 'youtube_api', 'crawler'

ALTER TABLE courses
  ADD COLUMN IF NOT EXISTS auto_detected_at TIMESTAMPTZ;
  -- 자동 수집된 시각 기록용
```

---

## 4. Vercel 배포 연결

### 4-1. `vercel.json` 파일 생성

루트에 아래 내용의 `vercel.json` 파일을 만듭니다.

```json
{
  "version": 2,
  "builds": [{ "src": "index.html", "use": "@vercel/static" }],
  "routes": [{ "src": "/(.*)", "dest": "/index.html" }]
}
```

### 4-2. Vercel 연결 순서

1. vercel.com 로그인 → **Add New Project**
2. GitHub 연결 → 생성한 `igangui` 리포지토리 선택
3. Framework Preset: **Other** 선택
4. **Deploy** 클릭
5. 완료 후 자동 생성된 URL (예: `igangui.vercel.app`)로 접속 확인

### 4-3. 이후 배포 방식

GitHub에 코드를 push(업로드)하면 Vercel이 자동으로 감지하여 **30초 내에 배포를 새로 진행합니다.** 별도 작업이 필요 없습니다.

---

## 5. 자동화 파이프라인 구축

강의 데이터를 자동으로 수집하여 Supabase `courses` 테이블에 등록하는 2가지 방법을 구현합니다.

---

### 방법 A — YouTube API + Make.com (메인, 추천 ⭐⭐⭐)

코딩 없이 GUI로 구성 가능한 방법입니다. 유튜브 채널의 신규 영상/커뮤니티 게시글을 감지하여 강의 정보를 자동 등록합니다.

#### Step 1. YouTube Data API v3 키 발급

1. [console.cloud.google.com](https://console.cloud.google.com) 접속
2. 새 프로젝트 생성 (이름 예: `igangui-auto`)
3. API 및 서비스 → 라이브러리 → **YouTube Data API v3** 검색 → 사용 설정
4. 사용자 인증 정보 → **API 키 생성**
5. 생성된 키를 복사하여 보관 (이후 Make.com에서 사용)

#### Step 2. Make.com 시나리오 구성

Make.com에서 새 시나리오를 만들어 아래 순서로 모듈을 연결합니다.

| 순서 | 모듈 | 설정 내용 |
|------|------|----------|
| 1 | Schedule (트리거) | 매일 오전 9시 실행 |
| 2 | HTTP - Make a Request | YouTube API로 채널 영상 목록 조회 |
| 3 | Iterator | 영상 목록을 하나씩 순회 |
| 4 | Filter (조건 분기) | 제목에 `강의`, `클래스`, `오픈` 포함 여부 확인 |
| 5 | Supabase - Insert Row | courses 테이블에 신규 강의 정보 INSERT |

#### Step 3. YouTube API 호출 URL 형식

```
GET https://www.googleapis.com/youtube/v3/search
  ?key={YOUR_API_KEY}
  &channelId={CHANNEL_ID}
  &part=snippet
  &order=date
  &maxResults=10
```

> 💡 **팁:** `channelId`는 유튜버 채널 페이지에서 우클릭 → 페이지 소스 보기 후 `channelId` 검색으로 찾을 수 있습니다.

#### Step 4. Supabase Insert 필드 매핑

Make.com의 Supabase 모듈에서 아래 필드를 매핑합니다.

| Supabase 필드 | Make.com 매핑값 |
|--------------|----------------|
| `title` | `{{snippet.title}}` (YouTube 영상 제목) |
| `creator_id` | 하드코딩 (해당 크리에이터 UUID) |
| `source` | `'youtube_api'` 고정값 |
| `auto_detected_at` | `{{now}}` (현재 시각) |

---

### 방법 B — Python 크롤러 + GitHub Actions (보조)

클래스101, 패스트캠퍼스 등 **유튜브 외 플랫폼**의 강의를 감지할 때 사용합니다.  
GitHub Actions로 스케줄 실행하므로 **별도 서버가 필요 없습니다.**

#### Step 1. `automation/crawler.py` 파일 작성

```python
import requests, os
from bs4 import BeautifulSoup

SUPABASE_URL = os.environ['SUPABASE_URL']
SUPABASE_SERVICE_KEY = os.environ['SUPABASE_SERVICE_KEY']

# 크롤링할 크리에이터 & 강의 페이지 목록
TARGETS = [
    {
        'creator_id': 'UUID-여기에-입력',
        'url': 'https://class101.net/ko/channels/채널명',
    },
]

def crawl_and_insert(target):
    res = requests.get(target['url'], timeout=10)
    soup = BeautifulSoup(res.text, 'html.parser')

    # 강의 제목 파싱 (사이트 구조에 따라 수정 필요)
    title_tag = soup.find('h1')
    if not title_tag:
        print(f"제목 파싱 실패: {target['url']}")
        return

    title = title_tag.text.strip()

    # Supabase courses 테이블에 INSERT
    response = requests.post(
        f"{SUPABASE_URL}/rest/v1/courses",
        headers={
            'apikey': SUPABASE_SERVICE_KEY,
            'Authorization': f'Bearer {SUPABASE_SERVICE_KEY}',
            'Content-Type': 'application/json',
            'Prefer': 'return=minimal',
        },
        json={
            'title': title,
            'creator_id': target['creator_id'],
            'source': 'crawler',
        }
    )

    if response.status_code in (200, 201):
        print(f"등록 성공: {title}")
    else:
        print(f"등록 실패 ({response.status_code}): {response.text}")

for target in TARGETS:
    crawl_and_insert(target)
```

#### Step 2. `automation/requirements.txt` 파일 작성

```
requests
beautifulsoup4
```

#### Step 3. `.github/workflows/crawler.yml` 파일 작성

리포지토리 루트에 `.github/workflows/` 폴더를 만들고 아래 파일을 생성합니다.

```yaml
name: Weekly Crawler

on:
  schedule:
    - cron: '0 0 * * 1'  # 매주 월요일 오전 9시 KST (UTC 0시)
  workflow_dispatch:       # 수동 실행 버튼도 활성화

jobs:
  crawl:
    runs-on: ubuntu-latest
    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v3

      - name: Python 설정
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 의존성 설치
        run: pip install -r automation/requirements.txt

      - name: 크롤러 실행
        run: python automation/crawler.py
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
```

#### Step 4. GitHub Secrets 등록

1. GitHub 리포지토리 → **Settings** → **Secrets and variables** → **Actions**
2. **New repository secret** 클릭
3. `SUPABASE_URL` 추가: Supabase 프로젝트 URL (예: `https://tctogg...supabase.co`)
4. `SUPABASE_SERVICE_KEY` 추가: Supabase 대시보드 → Project Settings → API → **service_role** 키

> ⚠️ **주의:** `service_role` key는 anon key와 완전히 다른 비밀 키로, RLS를 우회하는 전체 권한을 가집니다. 절대 외부에 공개하지 마세요.

---

## 6. 전체 작업 순서 요약

처음 개발을 시작하는 경우, **아래 순서대로 진행**하면 됩니다.

| 순서 | 작업 | 예상 시간 |
|------|------|----------|
| 1 | GitHub 계정 생성 및 리포지토리 생성 | 15분 |
| 2 | 기존 index.html, vercel.json, .gitignore 파일 업로드 | 10분 |
| 3 | Vercel 계정 생성 및 GitHub 리포지토리 연결 | 15분 |
| 4 | Supabase 대시보드에서 RLS 정책 확인 및 보완 | 20분 |
| 5 | Supabase SQL Editor에서 source 컬럼 추가 SQL 실행 | 5분 |
| 6 | Google Cloud에서 YouTube Data API v3 키 발급 | 20분 |
| 7 | Make.com 계정 생성 및 시나리오 구성 | 40분 |
| 8 | automation/crawler.py 및 GitHub Actions 워크플로 파일 추가 | 30분 |
| 9 | GitHub Secrets 등록 (SUPABASE_SERVICE_KEY 등) | 5분 |
| 10 | 전체 동작 테스트 (수동 실행 후 Supabase DB 확인) | 20분 |

**총 예상 시간: 약 3시간**

---

## 7. 기존 기능 유지 확인 체크리스트

Vercel 배포 후 아래 항목을 반드시 직접 클릭하며 확인합니다.

- [ ] 유튜버명 검색 → 크리에이터 카드 + 강의 목록 표시
- [ ] 평균 만족도, 추천율(도움됨/안됨 %) 표시
- [ ] 가중치 태그(긍정/부정/중립) 집계 및 색상 표시
- [ ] '수강생 후기 보기' 토글 → 후기 목록 펼치기/접기
- [ ] '이 강의 후기 남기기' 버튼 → 모달 팝업 정상 열림
- [ ] 모달 Step 1: 별점 선택, 추천여부 선택, 한마디 입력
- [ ] 모달 금칙어 필터 동작 (사기, 불법 등 입력 시 경고창)
- [ ] 모달 Step 2: 태그 최소 3개 선택 강제 (미선택 시 차단)
- [ ] 후기 최종 등록 후 Supabase `reviews` 테이블에 데이터 저장 확인
- [ ] 채널 링크가 있는 크리에이터는 '🔗 채널보기' 버튼 표시
- [ ] 검색 결과 없을 때 '이메일로 요청' 안내 문구 표시
- [ ] 모바일(스마트폰)에서 레이아웃 정상 출력 확인

---

## 8. 보안 권장사항

| 항목 | 현재 | 권장 조치 |
|------|------|----------|
| Anon Key | index.html에 하드코딩 | Vercel 환경변수로 이동 (선택 사항) |
| Service Role Key | 없음 | GitHub Secrets에만 저장, 절대 코드에 포함 금지 |
| RLS | 미확인 | courses INSERT는 service_role만 허용 |
| 중복 강의 방지 | 없음 | courses 테이블에 UNIQUE 제약 추가 |

> 💡 **참고:** Supabase anon key는 RLS가 올바르게 설정되어 있다면 공개되어도 큰 문제가 없습니다. 프론트엔드에서 사용하도록 설계된 키입니다.

### 중복 강의 방지 SQL

같은 크리에이터의 동일 제목 강의가 중복 등록되지 않도록 제약을 추가합니다.  
**Supabase SQL Editor에서 실행:**

```sql
ALTER TABLE courses
ADD CONSTRAINT unique_course_per_creator
UNIQUE (creator_id, title);
```

---

## 9. 참고 자료 및 공식 문서

| 주제 | 링크 |
|------|------|
| GitHub 시작 가이드 (한국어) | https://docs.github.com/ko |
| Vercel 배포 문서 | https://vercel.com/docs |
| Supabase RLS 가이드 | https://supabase.com/docs/guides/auth/row-level-security |
| YouTube Data API v3 | https://developers.google.com/youtube/v3 |
| Make.com 시작하기 | https://make.com/en/help |
| GitHub Actions 문서 | https://docs.github.com/ko/actions |

---

*— 끝 —*
