# 🎙 제작자 공식 답변 기능 — 개발 지시서 v2

> **기존 기능 전혀 건드리지 않음 | 추가만 함** | 2026년 2월 작성

---

## 목차

- [0. 기획 의도 및 명칭 원칙](#0-기획-의도-및-명칭-원칙)
- [1. 전체 기능 구조 설계](#1-전체-기능-구조-설계)
- [2. Supabase DB 작업](#2-supabase-db-작업)
- [3. 답변 작성 페이지 (reply.html)](#3-답변-작성-페이지-replyhtml)
- [4. index.html 수정 — 답변 UI 표시](#4-indexhtml-수정--답변-ui-표시)
- [5. 운영자 처리 흐름 (삭제 요청 대응 매뉴얼)](#5-운영자-처리-흐름-삭제-요청-대응-매뉴얼)
- [6. 작업 순서 요약](#6-작업-순서-요약)
- [7. 기존 기능 유지 확인 체크리스트](#7-기존-기능-유지-확인-체크리스트)

---

## 0. 기획 의도 및 명칭 원칙

### 이 기능은 "반박 도구"가 아닙니다

제작자가 삭제를 요청할 때 무조건 지워주면 두 가지 문제가 생깁니다.

1. **사이트 신뢰도 하락** — 후기가 사라지면 사용자는 "돈 받고 지워주는 사이트"로 인식합니다.
2. **트래픽 기회 소실** — 후기가 많은 강의일수록 검색 유입이 높습니다. 삭제하면 그 트래픽이 사라집니다.

공식 답변 기능은 이 딜레마를 해결합니다. **제작자에게는 공식 입장 창구를, 사이트에는 신뢰와 트래픽을** 동시에 제공합니다.

### ✅ 반드시 써야 하는 명칭

| 쓸 것 | 예시 |
|-------|------|
| 제작자 공식 답변 | "🎙 제작자 공식 답변" |
| 공식 설명 등록 | "공식 설명을 등록하시겠어요?" |
| 공식 입장 | "본 답변은 제작자의 공식 입장입니다" |

### ❌ 절대 쓰지 말아야 하는 명칭

| 쓰지 말 것 | 이유 |
|-----------|------|
| 해명 | 잘못을 인정하는 뉘앙스, 방어적으로 보임 |
| 반박 | 수강생과 싸우는 구도로 보임 |
| 논란 대응 | 논란을 공식화하여 오히려 리스크 증가 |

---

## 1. 전체 기능 구조 설계

### 사용자 경험 흐름

```
[수강생] 후기 작성
    ↓
[사이트] 강의 카드 하단에 "제작자이신가요? 답변 등록 신청" 버튼 상시 노출
    ↓
[제작자] 신청 버튼 클릭 → 이메일 입력 → 신청 완료 화면
    ↓
[운영자] 이메일 확인 후 수동 승인 → 인증 링크가 담긴 이메일 발송
    ↓
[제작자] 링크 클릭 → reply.html에서 공식 답변 작성
    ↓
[사이트] 해당 강의 카드에 🎙 제작자 공식 답변 박스로 자동 표시
```

### 추가되는 구성요소

| 구성요소 | 역할 |
|---------|------|
| `creator_responses` 테이블 | 제작자 공식 답변 저장 |
| `creator_tokens` 테이블 | 인증 토큰 발급·검증 (72시간 1회용) |
| `creator_applications` 테이블 | 신청 이메일 및 운영자 승인 대기 내역 |
| `reply.html` (새 파일) | 제작자 전용 답변 작성 페이지 |
| `index.html` 수정 | 답변 UI + 신청 버튼 표시 (2곳만 수정) |

### 변경되지 않는 것

- `index.html`의 검색, 후기 보기, 후기 작성 모달 — **전혀 건드리지 않음**
- `reviews`, `courses`, `creator_stats` 테이블 구조 — **변경 없음**

---

## 2. Supabase DB 작업

**Supabase 대시보드 → SQL Editor에서 순서대로 실행합니다.**

### 2-1. `creator_responses` 테이블 생성

```sql
CREATE TABLE creator_responses (
  id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  course_id     UUID NOT NULL REFERENCES courses(id) ON DELETE CASCADE,
  creator_id    UUID NOT NULL,
  content       TEXT NOT NULL CHECK (char_length(content) BETWEEN 10 AND 1000),
  is_visible    BOOLEAN NOT NULL DEFAULT true,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- 강의 1개당 답변은 1개만 허용
CREATE UNIQUE INDEX one_response_per_course
  ON creator_responses (course_id, creator_id);

CREATE INDEX idx_creator_responses_course
  ON creator_responses (course_id);
```

### 2-2. `creator_tokens` 테이블 생성

```sql
CREATE TABLE creator_tokens (
  id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  token        TEXT NOT NULL UNIQUE DEFAULT encode(gen_random_bytes(32), 'hex'),
  creator_id   UUID NOT NULL,
  course_id    UUID NOT NULL,
  used         BOOLEAN NOT NULL DEFAULT false,
  expires_at   TIMESTAMPTZ NOT NULL DEFAULT (now() + INTERVAL '72 hours'),
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_creator_tokens_token
  ON creator_tokens (token)
  WHERE used = false AND expires_at > now();
```

### 2-3. `creator_applications` 테이블 생성

제작자가 신청 버튼을 눌러 이메일을 입력했을 때 저장하는 테이블입니다.  
운영자가 이 테이블을 보고 수동으로 토큰을 발급합니다.

```sql
CREATE TABLE creator_applications (
  id           UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  course_id    UUID NOT NULL REFERENCES courses(id) ON DELETE CASCADE,
  email        TEXT NOT NULL,
  status       TEXT NOT NULL DEFAULT 'pending'
                 CHECK (status IN ('pending', 'approved', 'rejected')),
  created_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX idx_creator_applications_status
  ON creator_applications (status, created_at DESC);
```

### 2-4. RLS 정책 설정

```sql
-- creator_responses: 공개 답변은 누구나 읽기 가능
ALTER TABLE creator_responses ENABLE ROW LEVEL SECURITY;

CREATE POLICY "공개 답변 읽기"
  ON creator_responses FOR SELECT
  USING (is_visible = true);

-- creator_tokens: 외부 직접 접근 전면 차단
ALTER TABLE creator_tokens ENABLE ROW LEVEL SECURITY;

-- creator_applications: 신청서 INSERT만 허용 (읽기 차단)
ALTER TABLE creator_applications ENABLE ROW LEVEL SECURITY;

CREATE POLICY "신청서 제출"
  ON creator_applications FOR INSERT
  WITH CHECK (true);
```

### 2-5. 서버 측 함수 3개 생성

보안을 위해 민감한 로직은 모두 Supabase 내부 함수로 처리합니다.

```sql
-- ① 강의별 공식 답변 조회 (index.html에서 호출)
CREATE OR REPLACE FUNCTION get_creator_response(target_course_id UUID)
RETURNS TABLE (content TEXT, created_at TIMESTAMPTZ)
LANGUAGE sql STABLE AS $$
  SELECT content, created_at
  FROM creator_responses
  WHERE course_id = target_course_id
    AND is_visible = true
  LIMIT 1;
$$;

-- ② 토큰 검증 (reply.html 진입 시 호출)
CREATE OR REPLACE FUNCTION verify_creator_token(input_token TEXT)
RETURNS JSON
LANGUAGE plpgsql SECURITY DEFINER AS $$
DECLARE
  rec creator_tokens%ROWTYPE;
BEGIN
  SELECT * INTO rec
  FROM creator_tokens
  WHERE token = input_token
    AND used = false
    AND expires_at > now()
  LIMIT 1;

  IF NOT FOUND THEN
    RETURN json_build_object('error', true, 'message', '유효하지 않은 링크입니다');
  END IF;

  RETURN json_build_object(
    'creator_id', rec.creator_id,
    'course_id',  rec.course_id
  );
END;
$$;

-- ③ 답변 등록 (토큰 소비 + INSERT 원자적 처리)
CREATE OR REPLACE FUNCTION submit_creator_response(
  input_token   TEXT,
  input_content TEXT
)
RETURNS JSON
LANGUAGE plpgsql SECURITY DEFINER AS $$
DECLARE
  rec creator_tokens%ROWTYPE;
BEGIN
  SELECT * INTO rec
  FROM creator_tokens
  WHERE token = input_token
    AND used = false
    AND expires_at > now()
  LIMIT 1;

  IF NOT FOUND THEN
    RETURN json_build_object('success', false, 'message', '링크가 만료되었거나 이미 사용되었습니다');
  END IF;

  INSERT INTO creator_responses (course_id, creator_id, content)
  VALUES (rec.course_id, rec.creator_id, input_content)
  ON CONFLICT (course_id, creator_id) DO UPDATE
    SET content = EXCLUDED.content, updated_at = now();

  -- 토큰 1회 소비
  UPDATE creator_tokens SET used = true WHERE token = input_token;

  RETURN json_build_object('success', true);
END;
$$;
```

---

## 3. 답변 작성 페이지 (`reply.html`)

리포지토리 루트에 `reply.html`을 **새로 생성**합니다. `index.html`은 건드리지 않습니다.

```html
<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>제작자 공식 답변 등록 | 이 강의 어때?</title>
  <style>
    * { box-sizing: border-box; }
    body {
      font-family: -apple-system, sans-serif;
      background: #f8f9fa;
      margin: 0;
      display: flex;
      justify-content: center;
      padding: 40px 20px;
      min-height: 100vh;
    }
    .card {
      background: white;
      border-radius: 24px;
      padding: 30px;
      max-width: 440px;
      width: 100%;
      box-shadow: 0 10px 30px rgba(0,0,0,0.08);
      height: fit-content;
    }
    .logo { font-size: 14px; color: #aaa; margin-bottom: 24px; }
    h2 { font-size: 20px; margin: 0 0 6px; color: #1a1a1a; }
    .sub { font-size: 13px; color: #888; margin-bottom: 24px; line-height: 1.6; }
    .course-box {
      background: #f0f4ff;
      border-radius: 12px;
      padding: 14px 16px;
      font-size: 14px;
      font-weight: bold;
      color: #333;
      margin-bottom: 20px;
    }
    .course-label { font-size: 11px; color: #888; font-weight: normal; margin-bottom: 4px; }
    .guide {
      border: 1px solid #e8e8e8;
      border-radius: 12px;
      padding: 14px 16px;
      font-size: 13px;
      color: #555;
      margin-bottom: 20px;
      line-height: 1.8;
    }
    .guide-title { font-weight: bold; color: #333; margin-bottom: 8px; font-size: 13px; }
    textarea {
      width: 100%;
      height: 180px;
      border: 2px solid #e0e0e0;
      border-radius: 14px;
      padding: 14px;
      font-size: 14px;
      line-height: 1.7;
      resize: none;
      outline: none;
      transition: border-color 0.2s;
      font-family: -apple-system, sans-serif;
    }
    textarea:focus { border-color: #007bff; }
    .char-count { text-align: right; font-size: 12px; color: #bbb; margin: 6px 0 20px; }
    /* 법적 고지 박스 */
    .legal-box {
      background: #fafafa;
      border: 1px solid #e8e8e8;
      border-radius: 10px;
      padding: 12px 14px;
      font-size: 11px;
      color: #999;
      line-height: 1.7;
      margin-bottom: 20px;
    }
    .submit-btn {
      width: 100%;
      background: #007bff;
      color: white;
      border: none;
      padding: 16px;
      border-radius: 14px;
      font-size: 15px;
      font-weight: bold;
      cursor: pointer;
      transition: background 0.2s;
    }
    .submit-btn:hover { background: #0069d9; }
    .submit-btn:disabled { background: #ccc; cursor: not-allowed; }
    .error-msg { color: #ff4d4f; font-size: 13px; text-align: center; margin-top: 12px; min-height: 20px; }
    /* 완료 / 오류 화면 */
    .result {
      display: none;
      text-align: center;
      padding: 20px 0;
      line-height: 1.8;
    }
    .result-icon { font-size: 52px; margin-bottom: 16px; }
    .result-title { font-size: 18px; font-weight: bold; color: #1a1a1a; margin-bottom: 8px; }
    .result-sub { font-size: 14px; color: #888; }
  </style>
</head>
<body>
<div class="card">

  <!-- 로딩 -->
  <div id="loading" style="text-align:center; padding:60px 0; color:#aaa; font-size:14px;">
    인증 확인 중...
  </div>

  <!-- 답변 작성 폼 -->
  <div id="form-area" style="display:none;">
    <div class="logo">🤔 이 강의 어때?</div>
    <h2>🎙 제작자 공식 답변 등록</h2>
    <p class="sub">
      본인 인증이 완료된 제작자만 이 페이지에 접근할 수 있습니다.<br>
      작성하신 내용은 해당 강의 페이지에 공식 답변으로 표시됩니다.
    </p>

    <div class="course-box">
      <div class="course-label">답변 대상 강의</div>
      <div id="course-title-box">강의명 불러오는 중...</div>
    </div>

    <div class="guide">
      <div class="guide-title">작성 가이드</div>
      ✅ 수강생의 경험을 존중하는 톤으로 작성해주세요<br>
      ✅ 개선 내용이나 지원 사항을 구체적으로 알려주시면 좋습니다<br>
      ❌ 수강생을 특정하거나 비방하는 내용은 비공개 처리됩니다
    </div>

    <textarea
      id="reply-text"
      maxlength="1000"
      placeholder="예) 수강해주신 분들께 먼저 감사드립니다. 말씀해주신 부분에 대해 설명드리겠습니다..."
      oninput="updateCount()"
    ></textarea>
    <div class="char-count"><span id="char-count">0</span> / 1000</div>

    <!-- 법적 고지 (필수 표시) -->
    <div class="legal-box">
      본 답변은 제작자가 직접 등록한 공식 입장입니다.<br>
      운영자는 내용의 사실 여부를 보증하지 않습니다.
    </div>

    <button class="submit-btn" id="submit-btn" onclick="submitReply()">
      공식 답변 등록하기
    </button>
    <p class="error-msg" id="error-msg"></p>
  </div>

  <!-- 등록 완료 화면 -->
  <div class="result" id="result-area">
    <div class="result-icon">✅</div>
    <div class="result-title">공식 답변이 등록되었습니다</div>
    <div class="result-sub">강의 페이지에 제작자 공식 답변으로 표시됩니다.</div>
  </div>

  <!-- 오류 화면 -->
  <div class="result" id="error-area">
    <div class="result-icon">❌</div>
    <div class="result-title" id="error-area-msg">유효하지 않은 링크입니다</div>
    <div class="result-sub">
      링크가 만료되었거나 이미 사용된 링크입니다.<br>
      운영팀으로 문의해 주세요.
    </div>
  </div>

</div>

<script>
  const SUPABASE_URL = 'https://tctogguvgwvkxpymdsmn.supabase.co';
  const SUPABASE_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InRjdG9nZ3V2Z3d2a3hweW1kc21uIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjgwNzA0MzQsImV4cCI6MjA4MzY0NjQzNH0.sd54qY83b09GmsIKVWCB7IkjLrC1vePrhbhPHKnQazE';
  const headers = {
    'apikey': SUPABASE_KEY,
    'Authorization': 'Bearer ' + SUPABASE_KEY,
    'Content-Type': 'application/json'
  };

  const token = new URLSearchParams(window.location.search).get('token');

  // ── 페이지 진입: 토큰 검증 ──────────────────────────────────
  window.addEventListener('DOMContentLoaded', async () => {
    if (!token) return showError('토큰이 없습니다.');

    const res = await fetch(`${SUPABASE_URL}/rest/v1/rpc/verify_creator_token`, {
      method: 'POST', headers,
      body: JSON.stringify({ input_token: token })
    });
    const data = await res.json();

    if (!data || data.error || !data.course_id) return showError('유효하지 않은 링크입니다.');

    // 강의명 표시
    const courseRes = await fetch(
      `${SUPABASE_URL}/rest/v1/courses?id=eq.${data.course_id}&select=title`,
      { headers }
    );
    const courses = await courseRes.json();
    document.getElementById('course-title-box').textContent =
      courses[0]?.title || '강의명을 불러올 수 없습니다.';

    document.getElementById('loading').style.display = 'none';
    document.getElementById('form-area').style.display = 'block';
  });

  // ── 글자 수 카운터 ──────────────────────────────────────────
  function updateCount() {
    document.getElementById('char-count').textContent =
      document.getElementById('reply-text').value.length;
  }

  // ── 답변 등록 ──────────────────────────────────────────────
  async function submitReply() {
    const content = document.getElementById('reply-text').value.trim();
    const errEl   = document.getElementById('error-msg');

    if (content.length < 10) {
      errEl.textContent = '답변은 10자 이상 입력해주세요.';
      return;
    }

    const btn = document.getElementById('submit-btn');
    btn.disabled = true;
    btn.textContent = '등록 중...';
    errEl.textContent = '';

    const res = await fetch(`${SUPABASE_URL}/rest/v1/rpc/submit_creator_response`, {
      method: 'POST', headers,
      body: JSON.stringify({ input_token: token, input_content: content })
    });
    const result = await res.json();

    if (result?.success) {
      document.getElementById('form-area').style.display = 'none';
      document.getElementById('result-area').style.display = 'block';
    } else {
      errEl.textContent = result?.message || '오류가 발생했습니다. 다시 시도해주세요.';
      btn.disabled = false;
      btn.textContent = '공식 답변 등록하기';
    }
  }

  function showError(msg) {
    document.getElementById('loading').style.display = 'none';
    document.getElementById('error-area-msg').textContent = msg;
    document.getElementById('error-area').style.display = 'block';
  }
</script>
</body>
</html>
```

---

## 4. `index.html` 수정 — 답변 UI 표시

기존 코드에서 **2곳만 수정**합니다.

### 4-1. CSS 추가

`<style>` 태그 안 **맨 끝**에 아래를 붙여넣습니다.

```css
/* ── 제작자 공식 답변 박스 ── */
.reply-section {
  margin-top: 20px;
  border-top: 1px dashed #e0e0e0;
  padding-top: 16px;
}
.reply-section-title {
  font-size: 12px;
  font-weight: bold;
  color: #888;
  letter-spacing: 0.5px;
  margin-bottom: 12px;
}
/* 답변이 있을 때 */
.reply-box {
  background: #f8faff;
  border: 1.5px solid #c7d7fd;
  border-radius: 14px;
  padding: 16px;
}
.reply-badge {
  font-size: 11px;
  font-weight: bold;
  color: #007bff;
  background: #dbeafe;
  padding: 3px 10px;
  border-radius: 20px;
  display: inline-block;
  margin-bottom: 10px;
}
.reply-text {
  font-size: 14px;
  color: #333;
  line-height: 1.75;
  white-space: pre-wrap;
  word-break: break-all;
  margin-bottom: 10px;
}
.reply-legal {
  font-size: 11px;
  color: #bbb;
  line-height: 1.6;
  border-top: 1px solid #e8eef8;
  padding-top: 10px;
  margin-top: 4px;
}
.reply-date {
  font-size: 11px;
  color: #aaa;
  margin-top: 6px;
}
/* 답변이 없을 때 */
.reply-empty {
  text-align: center;
  padding: 16px 0 4px;
  color: #aaa;
  font-size: 13px;
  line-height: 1.7;
}
.apply-btn {
  display: inline-block;
  margin-top: 10px;
  font-size: 12px;
  color: #007bff;
  background: none;
  border: 1px solid #007bff;
  border-radius: 20px;
  padding: 5px 14px;
  cursor: pointer;
  text-decoration: none;
}
/* 신청 폼 (인라인) */
.apply-form {
  display: none;
  margin-top: 14px;
  text-align: left;
}
.apply-input {
  width: 100%;
  padding: 11px 14px;
  border: 1.5px solid #ddd;
  border-radius: 10px;
  font-size: 13px;
  outline: none;
  margin-bottom: 8px;
}
.apply-input:focus { border-color: #007bff; }
.apply-submit {
  width: 100%;
  background: #1a1a1a;
  color: white;
  border: none;
  padding: 12px;
  border-radius: 10px;
  font-size: 13px;
  font-weight: bold;
  cursor: pointer;
}
.apply-notice {
  font-size: 11px;
  color: #aaa;
  margin-top: 8px;
  line-height: 1.6;
}
```

### 4-2. `fetchCourses` 함수 수정

기존 `fetchCourses` 함수 안에서 **reviews fetch 바로 아래**에 공식 답변 조회를 추가하고, **카드 HTML 생성 부분** 마지막에 답변 UI를 추가합니다.

**① reviews fetch 바로 아래에 추가 (새 코드):**

```javascript
// ── [추가] 제작자 공식 답변 조회 ──────────────────────────────
const replyRes = await fetch(
  `${SUPABASE_URL}/rest/v1/rpc/get_creator_response`,
  { method: 'POST', headers, body: JSON.stringify({ target_course_id: c.id }) }
);
const replyRows = await replyRes.json();
const creatorReply = replyRows?.[0] || null;
// ────────────────────────────────────────────────────────────
```

**② 답변 UI HTML 변수 생성 (카드 HTML 생성 직전에 추가):**

```javascript
// 답변 있을 때
const replyHtml = creatorReply
  ? `<div class="reply-box">
       <span class="reply-badge">🎙 제작자 공식 답변 · 본인 인증 완료</span>
       <div class="reply-text">${creatorReply.content.replace(/</g,'&lt;').replace(/>/g,'&gt;')}</div>
       <div class="reply-date">${new Date(creatorReply.created_at).toLocaleDateString('ko-KR')} 등록</div>
       <div class="reply-legal">
         본 답변은 제작자가 직접 등록한 공식 입장입니다.<br>
         운영자는 내용의 사실 여부를 보증하지 않습니다.
       </div>
     </div>`
  // 답변 없을 때
  : `<div class="reply-empty">
       현재 등록된 공식 답변이 없습니다.
       <br>
       <button class="apply-btn" onclick="toggleApplyForm('${c.id}')">
         제작자이신가요? 답변 등록 신청
       </button>
       <div class="apply-form" id="apply-form-${c.id}">
         <input
           class="apply-input"
           id="apply-email-${c.id}"
           type="email"
           placeholder="비즈니스 이메일을 입력해주세요"
         />
         <button class="apply-submit" onclick="submitApplication('${c.id}')">
           신청하기
         </button>
         <div class="apply-notice">
           운영팀이 확인 후 입력하신 메일로 답변 링크를 발송드립니다.<br>
           통상 1~2 영업일 내 처리됩니다.
         </div>
       </div>
     </div>`;

const replySectionHtml = `
  <div class="reply-section">
    <div class="reply-section-title">🎙 제작자 공식 답변</div>
    ${replyHtml}
  </div>`;
```

**③ 카드 HTML의 닫는 `</div>` 바로 앞에 `${replySectionHtml}` 삽입:**

```javascript
list.insertAdjacentHTML('beforeend', `
  <div class="course-card">
    <strong class="course-title">${c.title}</strong>
    ...기존 HTML 그대로...
    <div id="reviews-${c.id}" class="review-content-area">${reviewsHtml || '첫 후기를 남겨주세요.'}</div>
    ${replySectionHtml}
  </div>`);
```

### 4-3. 신청 관련 함수 추가

`</script>` 태그 **바로 앞**에 아래 두 함수를 추가합니다.

```javascript
// 신청 폼 토글
function toggleApplyForm(courseId) {
  const form = document.getElementById(`apply-form-${courseId}`);
  form.style.display = form.style.display === 'block' ? 'none' : 'block';
}

// 신청서 제출
async function submitApplication(courseId) {
  const email = document.getElementById(`apply-email-${courseId}`).value.trim();
  if (!email || !email.includes('@')) {
    alert('올바른 이메일 주소를 입력해주세요.');
    return;
  }

  const res = await fetch(`${SUPABASE_URL}/rest/v1/creator_applications`, {
    method: 'POST',
    headers,
    body: JSON.stringify({ course_id: courseId, email })
  });

  if (res.status === 201) {
    document.getElementById(`apply-form-${courseId}`).innerHTML =
      `<div style="text-align:center; color:#52c41a; font-size:13px; padding:8px 0;">
         ✅ 신청이 접수되었습니다.<br>
         <span style="color:#aaa; font-size:11px;">운영팀 확인 후 메일로 안내드립니다.</span>
       </div>`;
  } else {
    alert('오류가 발생했습니다. 잠시 후 다시 시도해주세요.');
  }
}
```

---

## 5. 운영자 처리 흐름 (삭제 요청 대응 매뉴얼)

### 5-1. 신청 내역 확인 SQL

```sql
-- 신청 대기 목록 확인
SELECT
  a.id,
  a.email,
  a.created_at,
  c.title AS course_title,
  cs.creator_name
FROM creator_applications a
JOIN courses c ON c.id = a.course_id
JOIN creator_stats cs ON cs.creator_id = c.creator_id
WHERE a.status = 'pending'
ORDER BY a.created_at DESC;
```

### 5-2. 승인 및 토큰 발급 SQL

신청자의 이메일을 외부에서 확인한 뒤 (채널 공개 이메일 또는 유튜브 채널 소유자 인증 등), 운영자가 직접 승인합니다.

```sql
-- 1. 신청 상태를 승인으로 변경
UPDATE creator_applications
SET status = 'approved'
WHERE id = '신청-UUID-여기에';

-- 2. 토큰 발급
INSERT INTO creator_tokens (creator_id, course_id)
VALUES ('creator-UUID', 'course-UUID')
RETURNING token, expires_at;
-- 반환된 token을 복사해 아래 이메일에 붙여넣기
```

### 5-3. 제작자에게 보내는 이메일 템플릿

```
제목: [이 강의 어때?] 제작자 공식 답변 등록 링크

안녕하세요.

'이 강의 어때?' 운영팀입니다.

제작자 공식 답변 신청이 확인되었습니다.
아래 링크에서 직접 공식 입장을 등록해 주세요.

👉 https://igangui.vercel.app/reply.html?token={발급된_토큰}

• 링크 유효기간: 72시간
• 1회만 사용 가능합니다
• 등록 후 해당 강의 페이지에 즉시 반영됩니다

감사합니다.
이 강의 어때? 운영팀
```

### 5-4. 의사결정 플로우

```
제작자 삭제 요청 또는 신청 접수
        │
        ▼
    내용 검토
        │
  ┌─────┴──────────┐
허위사실·비방 포함?   개인 경험 후기
  │                   │
삭제 처리          공식 답변 안내
                   (토큰 발급 + 이메일)
                        │
                  제작자 답변 작성
                        │
                  사이트 자동 반영 ✅
```

---

## 6. 작업 순서 요약

| 순서 | 작업 | 예상 시간 |
|------|------|----------|
| 1 | Supabase SQL: `creator_responses` 테이블 생성 | 3분 |
| 2 | Supabase SQL: `creator_tokens` 테이블 생성 | 3분 |
| 3 | Supabase SQL: `creator_applications` 테이블 생성 | 3분 |
| 4 | Supabase SQL: RLS 정책 3개 설정 | 5분 |
| 5 | Supabase SQL: RPC 함수 3개 생성 | 10분 |
| 6 | 리포지토리에 `reply.html` 신규 생성 | 10분 |
| 7 | `index.html` — `<style>` 끝에 CSS 추가 | 5분 |
| 8 | `index.html` — `fetchCourses` 함수 수정 (2곳) | 10분 |
| 9 | `index.html` — `toggleApplyForm`, `submitApplication` 함수 추가 | 5분 |
| 10 | GitHub push → Vercel 자동 배포 확인 | 3분 |
| 11 | 테스트: 신청 버튼 → 이메일 입력 → Supabase 신청 내역 확인 | 5분 |
| 12 | 테스트: 토큰 발급 → reply.html → 답변 등록 → 카드에 표시 확인 | 10분 |

**총 예상 시간: 약 1시간 15분**

---

## 7. 기존 기능 유지 확인 체크리스트

배포 후 아래 항목을 반드시 직접 클릭하며 확인합니다.

**기존 기능 (변경 없음)**
- [ ] 유튜버명 검색 → 크리에이터 카드 정상 표시
- [ ] 강의 목록, 평균 만족도, 추천율 정상 표시
- [ ] 가중치 태그 집계 및 색상 표시 정상
- [ ] '수강생 후기 보기' 토글 정상 동작
- [ ] '이 강의 후기 남기기' 모달 정상 열림
- [ ] 후기 작성 2단계 정상 동작
- [ ] 금칙어 필터 정상 동작
- [ ] 후기 등록 후 Supabase `reviews` 테이블 저장 확인

**신규 기능**
- [ ] 답변 없는 강의: "현재 등록된 공식 답변이 없습니다" + 신청 버튼 표시
- [ ] 신청 버튼 클릭 → 이메일 입력 폼 표시
- [ ] 이메일 입력 후 신청 → Supabase `creator_applications` 테이블에 행 생성 확인
- [ ] 신청 완료 후 "✅ 신청이 접수되었습니다" 메시지 표시
- [ ] 답변 있는 강의: 🎙 제작자 공식 답변 박스 + 법적 고지 문구 표시
- [ ] `reply.html?token=잘못된값` → 오류 화면 표시
- [ ] `reply.html?token=올바른값` → 강의명 표시 + 작성 폼 표시
- [ ] 답변 등록 후 완료 화면 표시
- [ ] 동일 토큰으로 2회 시도 → 오류 메시지 표시
- [ ] 해명·반박·논란 등 금지 명칭이 UI 어디에도 없음 확인

---

*— 끝 —*
